/**
 * Method main of app, encharged of executing it.
 * @param args Array of strings for pass the corresopond arguments.
 * */
int main (string[] args) {
    var app = new PirateFilter.App ();
    return app.run (args);
}
